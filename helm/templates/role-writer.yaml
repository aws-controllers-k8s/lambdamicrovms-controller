---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-lambdamicrovms-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-lambdamicrovms-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-lambdamicrovms-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-lambdamicrovms-controller.chart.name-version" . }}
rules:
- apiGroups:
  - lambdamicrovms.services.k8s.aws
  resources:
  - microvms
  - microvmimages
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - lambdamicrovms.services.k8s.aws
  resources:
  - microvms
  - microvmimages
  verbs:
  - get
  - patch
  - update
